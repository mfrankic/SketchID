package com.mfrankic.sketchid;

public interface ExportableData {
  String getDrawingMode();
  // Consider adding getSessionId() if needed for more generic processing later
  // String getSessionId(); 
  // Consider adding getUserId() if needed
  // long getUserId();
} 
